console.log('destroy post called');

$('#post-<%= @post_id %>').remove();

new Noty({
    text: 'Post Deleted',
    theme: 'relax',
    type: 'success',
    layout: 'bottomLeft',
    timeout: 1000
}).show();