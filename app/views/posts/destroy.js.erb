console.log('destroy post called');

$('#post-<%= @post_id %>').remove();

new Noty({
    text: 'Post Deleted',
    theme: 'relax',
    type: 'warning',
    layout: 'topRight',
    timeout: 1000
}).show();