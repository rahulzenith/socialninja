console.log('destroy comment called');

$('#comment-<%= @comment_id %>').remove();

new Noty({
    text: 'Post Deleted',
    theme: 'relax',
    type: 'warning',
    layout: 'topRight',
    timeout: 1000
}).show();